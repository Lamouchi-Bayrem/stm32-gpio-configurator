# STM32 GPIO Configurator Professional

Qt 6 GUI that exports a MISRA-oriented, HAL-free STM32F4 GPIO driver, user configuration, unit tests, analysis script, and compliance-plan template.

## Build
```bash
cmake -S . -B build
cmake --build build --parallel
./build/STM32GPIOConfigurator
```
