# Generated STM32 GPIO project

Run `bash analysis/run-analysis.sh` for strict compilation, host unit tests, and Cppcheck when available. Integrate `Core/Inc` and `Core/Src` into a complete STM32 firmware project. Verify addresses and pins against the exact ST documentation.
